import "interop/axios.sharp";

function result fetch_text(string url) {
    return ok(get_text(url));
}

run {
    result current = fetch_text("https://example.com");

    if (is_ok(current)) {
        print("success");
        print(unwrap(current));
    } else {
        print(error_text(current));
    }
}
