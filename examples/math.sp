package examples;
module math;

function num add(num left, num right) {
    return left + right;
}

function text sharpen(text base) {
    sharp {
        var prefix = "sharp-";
        return join(prefix, base);
    }
}
