# Spear Language Extension

Syntax highlighting, snippets, and language configuration for `spear` source files.

- File extension: `.sp`
- Language id: `spear`
- Core memory concept: `sharp`

Open this folder as a VS Code extension project or package it with `vsce`.
